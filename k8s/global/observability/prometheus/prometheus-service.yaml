apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: ares-global
  labels:
    app: prometheus
spec:
  type: LoadBalancer  # ← EXTERNAL IP for GCP
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: web
  selector:
    app: prometheus