apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: ares-global
  labels:
    app: grafana
spec:
  type: LoadBalancer  # ← EXTERNAL IP for GCP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: web
  selector:
    app: grafana